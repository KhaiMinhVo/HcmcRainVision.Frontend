# Copy file này thành .env.local và điền URL ngrok của bạn.
# File .env.local không bị commit (đã có trong .gitignore).

# Backend qua ngrok – thay REPLACE_BY_NGROK_URL bằng URL khi chạy ngrok (vd: https://abc123.ngrok-free.app)
VITE_API_BASE_URL=REPLACE_BY_NGROK_URL
